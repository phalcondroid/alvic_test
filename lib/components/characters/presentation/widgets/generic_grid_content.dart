import 'package:flutter/material.dart';

class GenericGridContent extends StatelessWidget {
  const GenericGridContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(); // to do
  }
}