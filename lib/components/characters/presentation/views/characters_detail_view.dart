import 'package:flutter/material.dart';

class CharactersDetailView extends StatelessWidget {
  const CharactersDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}